# APEX SURVIVOR - アーキテクチャドキュメント

## 📐 システム構成図

### レイヤー構造

```
┌─────────────────────────────────────────────────────────┐
│         Entry Point (66 lines)                          │
│         chicken_game_ssd_ai.py                          │
│         - main() function only                          │
└─────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│         Core Game Logic Layer                           │
├─────────────────────────────────────────────────────────┤
│  core/game.py (1468 lines)                              │
│  - ChickenGame: ゲーム進行管理                           │
│  - トーナメント制御                                      │
│  - 環境管理                                              │
│                                                          │
│  core/player.py (1165 lines)                            │
│  - ChickenPlayer: プレイヤーAI                           │
│  - 意思決定エンジン                                      │
│  - 学習・適応                                            │
│                                                          │
│  core/state.py (63 lines)                               │
│  - PlayerState: プレイヤー状態管理                       │
└─────────────────────────────────────────────────────────┘
            │                    │
            ▼                    ▼
┌──────────────────┐   ┌──────────────────────────────────┐
│  Strategy Layer  │   │     SSD Theory Layer             │
├──────────────────┤   ├──────────────────────────────────┤
│ ssd_strategy.py  │   │  ssd/core.py                     │
│ (206 lines)      │   │  - SSDCore: SSD計算エンジン       │
│ - SSD戦略        │   │  - セマンティックジャンプ         │
│                  │   │  - 学習アルゴリズム               │
│ rule_strategy.py │   │                                   │
│ (225 lines)      │   │  ssd/state.py                    │
│ - ルール戦略     │   │  - SSDState: SSD状態管理          │
└──────────────────┘   └──────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────┐
│         Pressure Calculation Layer                      │
├─────────────────────────────────────────────────────────┤
│  pressure/rank_pressure.py                              │
│  - 順位からの圧力計算                                    │
│                                                          │
│  pressure/score_pressure.py                             │
│  - スコア差からの圧力計算                                │
│                                                          │
│  pressure/time_pressure.py                              │
│  - 時間経過からの圧力計算                                │
│                                                          │
│  pressure/survival_pressure.py                          │
│  - 生存状況からの圧力計算                                │
│                                                          │
│  pressure/overall_pressure.py                           │
│  - 総合圧力の統合                                        │
└─────────────────────────────────────────────────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────┐
│         Display Layer                                   │
├─────────────────────────────────────────────────────────┤
│  display/game_display.py (306 lines)                    │
│  - トーナメント結果表示                                  │
│  - ゲーム理論分析                                        │
│  - 統計情報                                              │
│                                                          │
│  display/colors.py                                      │
│  - カラー定義                                            │
│                                                          │
│  display/formatters.py                                  │
│  - フォーマット関数                                      │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 データフロー

### 1. ゲーム初期化フロー

```
main()
  │
  ├─ argparse でコマンドライン引数解析
  │   └─ --seed, --config
  │
  ├─ 乱数シード設定
  │   └─ random.seed(), np.random.seed()
  │
  └─ ChickenGame インスタンス作成
      │
      ├─ YAML設定ファイル読み込み
      │
      ├─ プレイヤー初期化 (×7)
      │   │
      │   ├─ ChickenPlayer作成
      │   │   ├─ SSDCore初期化
      │   │   ├─ SSDState初期化
      │   │   ├─ PlayerState初期化
      │   │   └─ Strategy選択
      │   │
      │   └─ 性格・パラメータ設定
      │
      └─ GameDisplay初期化
```

### 2. ラウンド実行フロー

```
play_tournament()
  │
  └─ for each set (1-5)
      │
      ├─ 環境選択フェーズ
      │   ├─ 各プレイヤーがAI投票
      │   └─ 多数決で環境決定
      │
      ├─ for each round (1-5)
      │   │
      │   ├─ 順位・スコア計算
      │   │
      │   ├─ 各プレイヤーの選択
      │   │   │
      │   │   ├─ 意味圧計算
      │   │   │   ├─ rank_pressure
      │   │   │   ├─ score_pressure
      │   │   │   ├─ time_pressure
      │   │   │   ├─ survival_pressure
      │   │   │   └─ overall_pressure
      │   │   │
      │   │   ├─ 戦略決定
      │   │   │   ├─ SSD戦略
      │   │   │   │   ├─ SSDCore.calculate()
      │   │   │   │   └─ 最適戦略選択
      │   │   │   │
      │   │   │   └─ ルール戦略
      │   │   │       └─ ルールベース選択
      │   │   │
      │   │   └─ 1-10の数字決定
      │   │
      │   ├─ クラッシュ判定
      │   │   └─ 確率計算 & 判定
      │   │
      │   ├─ 勝者決定
      │   │   └─ 最高値選択者
      │   │
      │   ├─ スコア計算
      │   │   └─ choice × 10 × 環境倍率
      │   │
      │   └─ HP・状態更新
      │
      ├─ セット結果表示
      │   └─ GameDisplay.show_results()
      │
      └─ HP購入フェーズ
          └─ 各プレイヤーがHP購入判断
```

### 3. 意思決定フロー（詳細）

```
ChickenPlayer.make_choice()
  │
  ├─ 1. 意味圧計算
  │   │
  │   ├─ calculate_meaning_pressure()
  │   │   ├─ 順位圧: (総人数 - 順位) / 総人数
  │   │   ├─ スコア差圧: gap / threshold
  │   │   ├─ 時間圧: remaining / total
  │   │   ├─ 生存圧: alive_count影響
  │   │   └─ 総合: α·順位 + β·スコア + γ·時間 + δ·生存
  │   │
  │   └─ P (meaning pressure) 算出
  │
  ├─ 2. SSD状態更新
  │   │
  │   ├─ SSDCore.update_state()
  │   │   ├─ E (未処理圧力) 更新
  │   │   ├─ T (温度) 調整
  │   │   ├─ κ (整合性) 学習
  │   │   └─ セマンティックジャンプ判定
  │   │
  │   └─ 戦略の動的調整
  │
  ├─ 3. 戦略選択
  │   │
  │   ├─ SSD戦略の場合
  │   │   ├─ SSDStrategy.make_choice()
  │   │   ├─ 性格重み適用
  │   │   ├─ ナッシュ均衡考慮
  │   │   └─ 確率的選択
  │   │
  │   └─ ルール戦略の場合
  │       ├─ RuleStrategy.make_choice()
  │       └─ 固定ルール適用
  │
  ├─ 4. 数値決定
  │   └─ 1-10の整数値を返す
  │
  └─ 5. コメント生成
      └─ 状況に応じたセリフ
```

---

## 🧩 主要クラス設計

### ChickenGame

```python
class ChickenGame:
    """ゲーム進行管理クラス"""
    
    # 属性
    config: dict           # ゲーム設定
    players: List[ChickenPlayer]  # プレイヤーリスト
    display: GameDisplay   # 表示管理
    environments: dict     # 環境定義
    
    # 主要メソッド
    __init__(config_path)  # 初期化
    play_tournament()      # トーナメント実行
    _play_set(set_num)     # セット実行
    _play_round(round_num) # ラウンド実行
    _select_environment()  # 環境選択
    _check_crash()         # クラッシュ判定
    _calculate_score()     # スコア計算
```

### ChickenPlayer

```python
class ChickenPlayer:
    """プレイヤーAIクラス"""
    
    # 属性
    state: PlayerState     # プレイヤー状態
    ssd_core: SSDCore      # SSDエンジン
    ssd_state: SSDState    # SSD状態
    strategy: str          # 戦略タイプ
    ssd_strategy: SSDStrategy      # SSD戦略
    rule_strategy: RuleStrategy    # ルール戦略
    
    # 主要メソッド
    __init__(...)          # 初期化
    make_choice(...)       # 選択決定
    _calculate_meaning_pressure(...) # 意味圧計算
    update_ssd(...)        # SSD更新
    _speak_choice(...)     # コメント生成
```

### SSDCore

```python
class SSDCore:
    """SSD理論計算エンジン"""
    
    # 属性
    strategies: dict       # 戦略定義
    kappa_init: float      # 初期整合性
    T_base: float          # 基本温度
    jump_threshold: float  # ジャンプ閾値
    
    # 主要メソッド
    initialize_state()     # 状態初期化
    update_state(...)      # 状態更新
    check_semantic_jump()  # ジャンプ判定
    update_kappa(...)      # 整合性学習
    update_temperature()   # 温度調整
```

---

## 📊 データモデル

### PlayerState (状態管理)

```python
@dataclass
class PlayerState:
    name: str              # プレイヤー名
    color: str             # 表示色
    personality: str       # 性格タイプ
    
    # SSDパラメータ
    kappa: Dict[str, float]  # 戦略整合性
    E: float                 # 未処理圧力
    T: float                 # 温度
    
    # ゲーム状態
    score: int              # ラウンドスコア
    total_score: int        # 累計スコア
    hp: int                 # 残りHP
    is_alive: bool          # 生存状態
    
    # 履歴
    round_scores: List[int]      # ラウンドごとのスコア
    history: List[int]           # 選択履歴
```

### SSDState (SSD状態)

```python
class SSDState:
    state: np.ndarray      # 状態ベクトル
    E: float               # 意味圧
    T: float               # 温度
    kappa: Dict[str, float]  # 整合性マップ
    last_strategy: str     # 前回戦略
    jump_count: int        # ジャンプ回数
```

---

## 🔧 設定システム

### chicken_game_config.yaml 構造

```yaml
game_rules:
  num_rounds: 5
  num_sets: 5
  starting_hp: 3
  hp_cost: 20

players:
  - name: "太郎"
    personality: "aggressive"
    strategy: "ssd"
    kappa: 0.5
    E_threshold: 5.0

environments:
  Normal:
    crash_rates: [0.02, 0.05, ...]
    score_multiplier: 1.0
  
ssd_theory:
  learning:
    eta: 0.1              # 学習率
    rho: 0.05             # 減衰率
    E_initial: 0.0
  
  semantic_jump:
    jump_threshold: 5.0
    jump_base_rate: 0.1
  
  meaning_pressure:
    alpha: 0.1            # 順位圧係数
    beta_E: 0.05          # スコア圧係数
```

---

## 🚦 制御フロー

### 環境選択アルゴリズム

```
1. 各プレイヤーがAI投票
   - 現在順位を分析
   - リスク許容度を計算
   - 環境を選択 (Normal/Safe/Deadly/...)

2. 投票集計
   - 多数決で決定
   - 同票の場合は個性重視

3. 環境パラメータ設定
   - クラッシュ率調整
   - 報酬倍率設定
```

### セマンティックジャンプ条件

```
if E > jump_threshold:
    jump_prob = jump_base_rate * exp(jump_gamma * (E - threshold))
    
    if random() < jump_prob:
        # 戦略を大胆に変更
        current_strategy → opposite_strategy
        E → E * 0.5  # 圧力リセット
        jump_count += 1
```

---

## 📈 拡張ポイント

### プラグイン可能な要素

1. **戦略モジュール**
   - `strategy/` ディレクトリに新規追加
   - BaseStrategy継承で実装

2. **意味圧計算**
   - `pressure/` ディレクトリに新規追加
   - カスタム圧力関数

3. **表示系**
   - `display/` ディレクトリに新規追加
   - GUI版の実装可能

4. **環境タイプ**
   - YAMLで定義追加
   - カスタム環境作成

---

## 🧪 テスト戦略

### テストレイヤー

```
Unit Tests (単体)
  ├─ test_player.py
  ├─ test_player_advanced.py
  └─ 各モジュールの機能検証

Integration Tests (統合)
  ├─ test_game.py
  ├─ test_game_advanced.py
  └─ エンドツーエンド検証

Performance Tests
  └─ 実行時間・メモリ使用量
```

---

## 📝 命名規則

- **クラス**: PascalCase (`ChickenGame`, `SSDCore`)
- **関数**: snake_case (`make_choice`, `calculate_pressure`)
- **定数**: UPPER_SNAKE_CASE (`MAX_ROUNDS`)
- **プライベート**: _prefix (`_internal_method`)

---

**Document Version: 1.0**  
**Last Updated: 2025-11-05**
